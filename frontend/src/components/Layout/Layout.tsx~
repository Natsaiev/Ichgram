import {Outlet, useLocation} from "react-router-dom";
import sideMenu from "../sideMenu/sideMenu.tsx";

const Layout = () => {
    const location = useLocation();

    const showSideMenu = ["/home", "/profile", "/search", ]

    return (
        <>

            <Outlet />
        </>
    );
};