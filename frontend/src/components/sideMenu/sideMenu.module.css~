.topMenu {
    position: fixed;
    top: 10px;
    right: 15px;
    display: flex;
    gap: 15px;
    transition: opacity 0.3s ease;
    z-index: 200;
}

.topMenu.hidden {
    opacity: 0;
    pointer-events: none;
}

.topMenu.visible {
    opacity: 1;
}

/* Боковое меню (ПК) / Нижнее меню (мобильное) */
.sideMenu {
    position: fixed;
    bottom: 0;
    left: 0;
    width: 100%;
    background: #fff;
    display: flex;
    justify-content: space-around;
    padding: 10px 0;
    border-top: 1px solid #ddd;
    z-index: 100;
}

.menuItem {
    text-decoration: none;
    color: #555;
    display: flex;
    flex-direction: column;
    align-items: center;
    font-size: 14px;
}

.menuItem img {
    width: 24px;
    height: 24px;
}

.logo {
    display: none;
}

/* Для ПК */
@media (min-width: 769px) {
    .sideMenu {
        flex-direction: column;
        width: 200px;
        height: 100vh;
        justify-content: flex-start;
        padding-top: 20px;
        border-right: 1px solid #ddd;
    }

    .menuItem {
        flex-direction: row;
        gap: 10px;
        font-size: 16px;
        padding: 10px 20px;
    }

    .logo {
        display: block;
        width: 150px;
        margin: 10px auto;
        margin: 30px 0;
    }

    .topMenu {
        display: none;
    }
}

/* Для мобильных устройств */
@media (max-width: 768px) {
    .topMenu {
        position: fixed;
        top: 10px;
        right: 10px;
        z-index: 200;
        gap: 10px;
    }

    .topMenu.hidden {
        display: none;
    }

    .sideMenu {
        position: fixed;
        bottom: 0;
        left: 0;
        width: 100%;
        background: #fff;
        display: flex;
        justify-content: space-around;
        padding: 10px 0;
        border-top: 1px solid #ddd;
    }

    .menuItem {
        display: flex; /* Показывать иконки */
        flex-direction: column;
        align-items: center;
    }

    .menuItem span {
        display: none; /* Скрыть текст */
    }

    .menuItem img {
        width: 30px;
        height: 30px;
    }
}